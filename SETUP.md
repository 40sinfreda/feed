# חיבור האפליקציה לגוגל שיטס

הגיליון כבר נוצר בחשבון גוגל שלך:
https://docs.google.com/spreadsheets/d/1rcqaYX2GSGMueIuHgcwxP9bw7QgDh41cLJxDkaaOHfo/edit

טוקן: `feed-2026-hadar`

## פריסת הגשר (פעם אחת, שתי דקות)

1. פתח את הגיליון.
2. **הרחבות → Apps Script**.
3. מחק את הקוד שב-`Code.gs` והדבק את הקובץ `Code.gs` מהריפו.
4. שמור (Ctrl+S).
5. **פריסה → פרוס כפרויקט אינטרנט**
   - תיאור: `feed-api`
   - ביצוע בתור: **אני**
   - גישה: **כל מי שיש לו את הקישור**
6. אשר הרשאות לחשבון גוגל.
7. העתק את כתובת ה-Web App (`.../exec`).
8. באפליקציה: **ניהול → הגדרות** → הדבק את הכתובת → **שמור וחבר לשיטס**.

בלי השלב הזה האפליקציה עובדת במכשיר (localStorage). אחריו כל המכשירים כותבים לאותו שיטס.
